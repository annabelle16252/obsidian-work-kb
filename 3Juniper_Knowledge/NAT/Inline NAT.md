# Inline NAT

> [!note] Recovered from OneNote
> This note was recovered from OneNote backup (2026-03-25).
> Content from Obsidian modifications after OneNote import may be missing.

Inline NAT
Services are performed in the forwarding plane​
Cons​
Still requires services pics for complex services​
Less flexibility due to limited resources​
Supports only IPv4 translation.
https://juniper.lightning.force.com/lightning/r/Knowledge__kav/ka0Dp000000jS5dIAE/view
Due to not having a Services card... ...(being MS-MIC or MS-MPC) or SPC3... then it cannot be configured CGNAT ...which is the high scale... NAT.... If still it is configured the cli prompt will return an error that "sp" interfaces are missing in configuration.
However there is low scale types of NAT that can be configured just by having 1 single MPC (trio) line card. These are all inline NAT, just the names change due to they have variances.
INLINE NAT Troubleshooting
INLINE NAT Troubleshooting
